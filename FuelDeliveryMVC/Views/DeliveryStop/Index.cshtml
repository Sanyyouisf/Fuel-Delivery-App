@model IEnumerable<FuelDeliveryMVC.Models.DeliveryStop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.customerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deliveryEventId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.noOfGallons)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.customerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deliveryEventId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.noOfGallons)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.deliveryStopId }) |
            @Html.ActionLink("Details", "Details", new { id=item.deliveryStopId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.deliveryStopId })
        </td>
    </tr>
}

</table>
